import { useState } from "react";
import styled from "@emotion/styled";

const Heading = styled.h1`
  font-family: "Lato", sans-serif;
  color: #fff;
`;

const App = () => {
  return <Heading>App js</Heading>;
};

export default App;
